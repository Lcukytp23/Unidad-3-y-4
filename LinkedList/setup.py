from setuptools import find_packages, setup

setup(
    name='mibliblio',
    version='0.1',
    packages=find_packages(),
    py_modules=["mibliblio"],  # Lista de dependencias si tu biblioteca las tiene
)
